[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pil-ingest"
version = "0.1.0"
description = "Feedback normalize/redact/cluster lib"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["ingest*"]
