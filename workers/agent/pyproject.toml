[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pil-agent"
version = "0.1.0"
description = "LangChain+LangGraph investigation agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["agent*"]
