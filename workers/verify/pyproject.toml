[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pil-verify"
version = "0.1.0"
description = "Post-deploy verification verdicts from error deltas"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["verify*"]
